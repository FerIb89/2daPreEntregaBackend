<div>
  <h1>Listado de Productos</h1>
</div>

<article id="list-products">
  {{#each docs}}
  <div class="cards-container">
    <div class="card" style="width: 16rem;">
      <img src="{{thumbnail}}" class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title">{{title}}</h5>
        <p class="card-text">{{this.description}}</p>
        <p class="card-text">Precio</p>
        <p class="card-text">U$D:{{this.price}}</p>
        <div class="card-body">
          <a  href="/{{this._id}}"  class="card-linkA">Ver mas</a>
          
        </div>
        <div class="add-quantity">
          <button type="submit" data-product-id="{{this._id}}" class="btn btn-primary add-to-cart-button">Añadir al
            Carrito</button>
          <div class="quantity-input">
            {{!-- <button type="button" class="quantity-button btn  quantity-up">+</button> --}}
            <input type="number" value="1" min="1" class="quantity-input-field">
            {{!-- <button type="button" class="quantity-button  btn quantity-down">-</button> --}}
          </div>
        </div>

      </div>
    </div>
  </div>
  {{/each}}


  
</article>

<div class="page">
  <div>
     {{#if hasPrevPage}}
  <a href="{{prevLink}}"  class="prev-page">Anterior</a>
  {{/if}}
  </div>
 
  <div>
    <p class="counter-page">
      {{page}} de {{totalPages}}
    </p>
  </div>
  <div >
    {{#if hasNextPage}}
    <a href="{{nextLink}}" class="next-page">Siguiente</i></a>
    {{/if}}
  </div>
</div>


  <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasRight" aria-labelledby="offcanvasRightLabel">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title" id="offcanvasRightLabel">Offcanvas right</h5>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body">
      ...
    </div>
  </div>




<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
<script src="js/cart.js" defer></script>